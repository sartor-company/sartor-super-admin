export type OnboardingRow = {
  _id?: string;
  client: string;
  location?: string;
  industry?: string;
  product: string;
  productLabel: string;
  tierLabel?: string;
  tierVariant?: string;
  step: string;
  stepDetail?: string;
  stepKey?: string;
  progress: number;
  progressColor?: string;
  stickerDesignRequired?: boolean;
  stickerDesignStatus?: 'na' | 'not_started' | 'pending' | 'approved' | 'ready_finalize';
  stickerDesignLabel?: string;
  stickerCanFinalize?: boolean;
  blocker: string;
  blockerColor?: string;
  assigned: string;
  assignedColor?: string;
  started: string;
  followUpMessage?: string;
  viewClientCode?: string;
  accountActivated?: boolean;
  action: 'followup' | 'view' | 'assign' | 'sticker';
};
